machine:
    python:
        version: 2.7.6

dependencies:
    override:
        - pip install -U tox virtualenv

test:
    override:
        - tox
    post:
        - pip install codecov
        - codecov --token=${CODECOV_TOKEN}
